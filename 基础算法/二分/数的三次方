#include<iostream>
#include<math.h>
using namespace std;
int main(){
    //1.所有数字和mid必须是double类型
    //double l=-10000, r=10000;
    double n;
    cin>>n;
    // while(r-l>1e-8){
    //     double mid=(l+r)/2;
    //2.mid^3不对，这是异或
    //     if(mid*mid*mid>=n)r=mid;
    //     else l=mid;
    // }
    
     /*cbrt函数*/
    double l=cbrt(n);
    printf("%lf\n", l);
    return 0;
    
}
